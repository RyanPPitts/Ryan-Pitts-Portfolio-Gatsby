import { GraphQLSchema } from 'graphql';
import { Transform } from './transforms';
import { SubschemaConfig, IGraphQLToolsResolveInfo } from '../Interfaces';
export default class CheckResultAndHandleErrors implements Transform {
    private context?;
    private info;
    private fieldName?;
    private subschema?;
    constructor(info: IGraphQLToolsResolveInfo, fieldName?: string, subschema?: GraphQLSchema | SubschemaConfig, context?: Record<string, any>);
    transformResult(result: any): any;
}
