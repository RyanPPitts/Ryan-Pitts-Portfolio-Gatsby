import { GraphQLSchema, FieldNode, SelectionNode, FragmentDefinitionNode } from 'graphql';
import { Request } from '../Interfaces';
import { Transform } from './transforms';
export declare type FieldNodeTransformer = (fieldNode: FieldNode, fragments: Record<string, FragmentDefinitionNode>) => SelectionNode | Array<SelectionNode>;
export declare type FieldNodeTransformerMap = {
    [typeName: string]: {
        [fieldName: string]: FieldNodeTransformer;
    };
};
export default class MapFields implements Transform {
    private schema;
    private fieldNodeTransformerMap;
    constructor(fieldNodeTransformerMap: FieldNodeTransformerMap);
    transformSchema(schema: GraphQLSchema): GraphQLSchema;
    transformRequest(originalRequest: Request): Request;
}
