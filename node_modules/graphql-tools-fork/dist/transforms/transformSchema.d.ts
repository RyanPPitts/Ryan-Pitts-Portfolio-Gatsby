import { GraphQLSchema } from 'graphql';
import { Transform } from '../transforms/transforms';
import { SubschemaConfig } from '../Interfaces';
export declare function wrapSchema(subschema: GraphQLSchema | SubschemaConfig, transforms?: Array<Transform>): GraphQLSchema;
export default function transformSchema(subschema: GraphQLSchema | SubschemaConfig, transforms: Array<Transform>): GraphQLSchema & {
    transforms: Array<Transform>;
};
